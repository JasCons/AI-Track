name: Debug Actions Runner

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Print debug
        run: |
          # print a simple debug line; use a block scalar to avoid YAML quoting issues
          echo 'Debug: Actions runner is working'
